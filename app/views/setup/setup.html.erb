<div class='account-setup-blurb'>
  <div class='account-setup-blurb-hi'>Hi. Nice to meet you!</div>
  <div>We'll need a bit of info from you, then let's get you a loan.</div>

</div>

<div class='ui center aligned segment loan-form'>
  <%= form_for(@user) do |f| %>
    <% if @user.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

        <ul>
        <% @user.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    <% end %>

    <div class="form-group">
      <%= f.label 'First Name *' %>
      <%= f.text_field :first_name, class: 'form-control', required: true %>
    </div>

    <div class="form-group">
      <%= f.label 'Last Name *' %>
      <%= f.text_field :last_name, class: 'form-control', required: true %>
    </div>

    <div class="form-group">
      <%= f.label 'Steam Email *' %>
      <%= f.email_field :email, class: 'form-control', required: true %>
    </div>

    <div class="form-group">
      <%= f.label 'Trade Url *' %>
      <%= f.text_field :trade_url, class: 'form-control', required: true %>
    </div>
    <a class='trade-link' href='http://steamcommunity.com/id/me/tradeoffers/privacy#trade_offer_access_url' target='_blank'>Find your Steam Trade Url here!</a>

    <div class="form-group">
      <%= f.label 'Phone Number' %>
      <%= f.text_field :phone_number, class: 'form-control' %>
    </div>

    <div class="form-group">
      <%= f.label 'Cashrep Url' %>
      <%= f.text_field :cashrep_url, class: 'form-control' %>
    </div>

    <div class="form-group">
      <%= f.label 'What’s your occupation outside of Steam?' %>
      <%= f.text_field :occupation, class: 'form-control' %>
    </div>

    <div class="form-group">
      <%= f.label 'Are you a trader in Steam?' %>
      <%= f.check_box :is_trader, class: '' %>
    </div>

    <div>(*) denotes required</div>
    <br>

    <%= f.submit 'Update', class: 'ui primary button' %>
  <% end %>
</div>
